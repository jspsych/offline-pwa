{
  "name": "@jspsych/offline-pwa-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky install",
    "build": "turbo run build",
    "test": "turbo run test",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:publish": "changeset publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.4.0",
    "@changesets/cli": "^2.16.0",
    "@jspsych/config": "^3.2.2",
    "@types/node": "^20.0.0",
    "husky": "^8.0.3",
    "lint-staged": "^15.0.0",
    "prettier": "^3.0.0",
    "turbo": "^1.12.0",
    "typescript": "^5.3.0"
  },
  "prettier": {
    "printWidth": 100
  },
  "lint-staged": {
    "*.{ts,js,mjs,cjs,json,md}": "prettier --write"
  }
}
